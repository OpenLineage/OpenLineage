workflows:
  openlineage-sap-datasphere:
    jobs:
      - unit-test-integration-sap-datasphere:
          filters:
            tags:
              only: /^[0-9]+(\.[0-9]+){2}(-rc\.[0-9]+)?$/
      - workflow_complete:
          requires:
            - unit-test-integration-sap-datasphere
