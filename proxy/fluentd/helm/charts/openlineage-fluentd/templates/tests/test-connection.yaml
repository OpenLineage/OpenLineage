apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openlineage-fluentd.fullname" . }}-test-connection"
  labels:
    {{- include "openlineage-fluentd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openlineage-fluentd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
