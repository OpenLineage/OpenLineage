 [{
 		"eventType": "START",
 		"inputs": [],
 		"job": {
 			"facets": {
 				"documentation": {
 					"description": "Test unknown_operator dag."
 				}
 			},
 			"name": "unknown_operator_dag.unknown_operator",
 			"namespace": "food_delivery"
 		},
 		"run": {
 			"facets": {
 				"airflow_runArgs": {
 					"externalTrigger": false
 				},
				"airflow": {
					"dag_run": "{{ any(result) }}",
					"dag": "{{ any(result) }}",
					"task_instance": "{{ any(result) }}",
					"task": "{{ any(result) }}",
					"task_uuid": "{{ any(result) }}"
				},
 				"parent": {
 					"job": {
 						"name": "unknown_operator_dag",
 						"namespace": "food_delivery"
 					}
 				},
				"unknownSourceAttribute": {
					"unknownItems": [
						{
							"name": "TestUnknownDummyOperator",
							"type": "operator",
							"properties": {}
						}
					]
				}
 			}
 		}
 	},
 	{
 		"eventType": "COMPLETE",
 		"inputs": [{
 			"facets": {},
 			"name": "dataset",
 			"namespace": "test"
 		}],
 		"job": {
 			"facets": {},
 			"name": "custom_extractor.custom_extractor",
 			"namespace": "food_delivery"
 		}
 	}
 ]
