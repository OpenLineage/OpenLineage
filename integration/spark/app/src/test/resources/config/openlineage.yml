transport:
  type: http
  url: http://localhost:1010
  endpoint: api/v1/lineage
  auth:
    type: api_key
    apiKey: random_token
jobName:
  appendDatasetName: false
facets:
  disabled: [aDisabledFacet]
  debug:
    enabled: true
  unknownEntry:
    enabled: true
  logicalPlan:
    enabled: true
incorrectEntry:
  shouldNotFail: true