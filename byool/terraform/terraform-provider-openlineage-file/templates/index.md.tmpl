---
page_title: "Provider: OpenLineage JSON File"
description: |-
  Records OpenLineage lineage events as local JSON files.
  A reference implementation for OpenLineage provider developers.
---

# OpenLineage JSON File Provider

The JSON file provider stores OpenLineage lineage events as local `.json` files.
It requires no external services, credentials, or network access —
its purpose is to serve as a **reference implementation** for developers
building their own OpenLineage Terraform providers.

## Example Usage

{{ tffile "examples/provider/provider.tf" }}

{{ .SchemaMarkdown | trimspace }}

