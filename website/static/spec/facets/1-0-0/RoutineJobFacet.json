{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openlineage.io/spec/facets/1-0-0/RoutineJobFacet.json",
  "$defs": {
    "RoutineJobFacet": {
      "allOf": [
        {
          "$ref": "https://openlineage.io/spec/2-0-2/OpenLineage.json#/$defs/JobFacet"
        },
        {
          "type": "object",
          "properties": {
            "namespace": {
              "description": "Routine's host system namespace, following the OpenLineage Naming convention used for datasets in the same system (e.g. `bigquery`, `snowflake://my-org-myaccount`, `redshift://cluster.us-east-1:5439`, `postgres://host:5432`). See https://openlineage.io/docs/spec/naming/",
              "type": "string"
            },
            "name": {
              "description": "Fully-qualified routine identifier inside the namespace, using the same path convention as tables in that system (e.g. `my-project.my_dataset.my_procedure` for BigQuery, `MY_DB.MY_SCHEMA.MY_PROCEDURE` for Snowflake)",
              "type": "string"
            },
            "routineType": {
              "description": "Kind of routine (`PROCEDURE`, `FUNCTION`, `SCALAR_FUNCTION`, `TABLE_VALUED_FUNCTION`, `AGGREGATE_FUNCTION`, `WINDOW_FUNCTION`, `MACRO`, `REMOTE_FUNCTION`, etc.)",
              "type": "string"
            },
            "language": {
              "description": "Implementation language of the routine body (`SQL`, `JAVASCRIPT`, `PYTHON`, `JAVA`, `SCALA`, `REMOTE`, etc.)",
              "type": "string"
            }
          },
          "required": ["namespace", "name"],
          "additionalProperties": true
        }
      ],
      "type": "object"
    }
  },
  "type": "object",
  "properties": {
    "routine": {
      "$ref": "#/$defs/RoutineJobFacet"
    }
  }
}
